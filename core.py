def multi(a, b):
    mom = float(a) * float(b)
    print(str(a) + ' * ' + str(b) + ' = ' + str(mom))

  
def divis(d, f):
    dom = float(d) / float(f)
    print(str(d) + "/" + str(f) + " = " str(dom)

        
def addit(e, j):
    aom = float(e) + float(j)
    print(str(e) + ' + ' + str(j) + ' = ' + str(aom))
        
        
def subtra(s, k):
    som = float(s) - float(k)
    print(str(s) + " - " + str(k) + " = " + str(som))
        
